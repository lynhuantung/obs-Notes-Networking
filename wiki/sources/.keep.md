# Sources

Thư mục này chứa các trang tóm tắt từng nguồn đã được ingest vào wiki.

**Chưa có source nào được ingest.**

## Cách ingest

Đặt file tài liệu vào `raw/` rồi nói với LLM:
> "ingest raw/ten-file.md"

LLM sẽ tạo trang `wiki/sources/<ten-file>.md` và cập nhật tất cả trang liên quan.
